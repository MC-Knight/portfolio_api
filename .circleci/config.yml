version: 2.1
jobs:
  build:
    docker:
      - image: node:18.18.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm run test
      - run:
          name: Build project
          command: npm run build
